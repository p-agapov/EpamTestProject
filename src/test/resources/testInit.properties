create_users=CREATE TABLE IF NOT EXISTS users ( user_id IDENTITY, login VARCHAR  NOT NULL, password VARCHAR, role VARCHAR);
create_customers=CREATE TABLE IF NOT EXISTS customers ( customer_id IDENTITY, name VARCHAR, surname VARCHAR, vip BOOLEAN, user_id INT );
create_tours=CREATE TABLE IF NOT EXISTS tours (tour_id IDENTITY, name VARCHAR, price INT, hot BOOLEAN, discount INT);
create_orders=CREATE TABLE IF NOT EXISTS orders (order_id IDENTITY, customer_id INT, tour_id INT, paid BOOLEAN);